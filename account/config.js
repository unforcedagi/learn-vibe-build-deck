// Supabase project coordinates for the class accounts page.
//
// Both values stay 'FILL_ME' until the cloud project exists — app.js detects
// the placeholders and shows a "not wired yet" banner instead of trying to
// connect, so this branch is safe to deploy as-is.
//
// Fill them from the Supabase dashboard (Settings > API) — see
// supabase/README.md for the full wiring order. The anon key is public by
// design (row-level security does the protecting); the service-role key must
// NEVER appear here.

export const SUPABASE_URL = 'FILL_ME';
export const SUPABASE_ANON_KEY = 'FILL_ME';
